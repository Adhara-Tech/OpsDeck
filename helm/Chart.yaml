apiVersion: v2
name: opsdeck
description: Helm chart for OpsDeck with support for Dual DB y Gateway API
type: application
version: 0.1.0
appVersion: "0.0.0"

dependencies:
  - name: postgresql
    version: 15.x.x
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled