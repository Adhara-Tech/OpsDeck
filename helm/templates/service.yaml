apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsdeck.fullname" . }}
  labels:
    {{- include "opsdeck.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsdeck.selectorLabels" . | nindent 4 }}